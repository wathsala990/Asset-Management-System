package com.example.AMS.controller.director;

/**
 * Deprecated placeholder to avoid mapping conflicts.
 * Replaced by L_AssetAllocationController which serves both the page and APIs.
 */
public class AssetAllocationPageController {
    // Intentionally empty; no Spring annotations
}
